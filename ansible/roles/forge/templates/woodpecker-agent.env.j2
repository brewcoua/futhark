# The agent's half of the shared secret. Its own file rather than the server's, so the agent's
# container is not handed the forge OAuth credential it has no use for.
WOODPECKER_AGENT_SECRET={{ secrets.brokkr.WOODPECKER_AGENT_SECRET }}
