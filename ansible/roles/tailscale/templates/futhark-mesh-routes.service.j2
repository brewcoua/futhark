[Unit]
Description=Futhark: route and masquerade pod traffic to Tailscale mesh peer IPs
After=tailscaled.service
Wants=tailscaled.service

[Service]
Type=oneshot
ExecStart=/usr/local/bin/futhark-mesh-routes.sh
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
