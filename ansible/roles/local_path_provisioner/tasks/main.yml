---
# openbao's StatefulSet needs the `local-path` StorageClass to bind its PVC on its very first
# reconcile, and nothing in the Flux-managed tree can provision it — openbao is the root of
# that dependency graph. See docs/src/conventions/ordering.md.
#
# Pinned to v0.0.36 (fixes a high-severity HelperPod template-injection CVE) for
# reproducibility — never track master.
- name: Install local-path-provisioner
  kubernetes.core.k8s:
    kubeconfig: "{{ generated_dir }}/kubeconfig"
    state: present
    src: https://raw.githubusercontent.com/rancher/local-path-provisioner/v0.0.36/deploy/local-path-storage.yaml
