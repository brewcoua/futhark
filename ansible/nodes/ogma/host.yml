---
node:
  hostname: ogma
  os: fedora
  workflow: podman
  mesh: true
  apps: [openbao, pocketid]
  ip: "{{ lookup('protonpass', 'pass://futharkd/ogma/ip address') }}"
  initial_user: fedora
  initial_port: 22
