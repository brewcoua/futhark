---
node:
  hostname: ogma
  os: fedora
  workflow: k0s
  k0s_role: worker
  mesh: true
  # Identifying, so the literal lives encrypted in host.sops.yml beside this file.
  ip: "{{ node_ip }}"
  initial_user: fedora
  initial_port: 22
