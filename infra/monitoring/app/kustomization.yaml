---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization.json
# One directory per workload — see docs/src/conventions/layout.md.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Shared by every element below, so it stays flat rather than picking one to live in.
  - helmrepositories.yaml
  - metrics
  - logs
  - exporters
  - grafana

components:
  - ../../../config/infisical
