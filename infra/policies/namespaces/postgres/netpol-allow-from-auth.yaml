---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/master-standalone-strict/networkpolicy-networking-v1.json
#
# One file per tenant. See netpol-allow-from-linkwarden.yaml for why this and the Database CR are
# added together.
#
# Pocket ID runs on ogma while this instance runs on kenaz, so unlike every other tenant here the
# traffic this opens crosses the mesh between nodes.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-from-auth
spec:
  podSelector: {}
  policyTypes: [Ingress]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: auth
      ports:
        - port: 5432
