---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrepository.yaml
  - helmrelease.yaml
  - secret.yaml
# Stamps the Infisical project/environment onto the secret — see config/infisical.
components:
  - ../../../config/infisical
