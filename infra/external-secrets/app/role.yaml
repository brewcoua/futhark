---
# ESO's own namespace: read access to the ServiceAccounts it token-reviews for the kubernetes
# auth method, and the openbao-ca ConfigMap its ClusterSecretStores reference as caProvider.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: external-secrets-controller
  namespace: external-secrets
rules:
  - apiGroups: [""]
    resources: [serviceaccounts, configmaps]
    verbs: [get, list, watch]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: external-secrets-controller
  namespace: external-secrets
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: external-secrets-controller
subjects:
  - kind: ServiceAccount
    name: external-secrets
    namespace: external-secrets
