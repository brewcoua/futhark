# Proton Pass references for `pass-cli run --env-file secrets.env -- tofu ...`.
# Only pass:// POINTERS (no secrets, no identifying values) — safe to commit.
BUNNYNET_API_KEY=pass://futharkd/bunny/api-key
TF_VAR_kenaz_public_ip=pass://futharkd/kenaz/public-ip
