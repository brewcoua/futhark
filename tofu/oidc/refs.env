# See tofu/bunny/refs.env for the format. References only, never values.

# The base domain and the internal subdomain label, for clients.tf's redirect URIs.
# config/dns/dns.sops.yaml is the single source — Flux substitutes the same keys into manifests.
TF_VAR_domain=config/dns/dns.sops.yaml#["stringData"]["DOMAIN"]
TF_VAR_sub_internal=config/dns/dns.sops.yaml#["stringData"]["SUB_INTERNAL"]
