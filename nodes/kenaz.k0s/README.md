# kenaz.k0s

Phase 2: apps land here, one directory per app: `<app>/ks.yaml` + `<app>/app/` — same
shape as `infra/infisical-operator/`. Add the app's directory to `resources:` in the
sibling `kustomization.yaml`.
